type Mutation {
  createNotification(
    eventId: ID!
    notification: NotificationInput!
  ): Notification

  updateNotification(
    eventId: ID!
    notificationId: ID!
    notification: NotificationInput!
  ): Notification

  removeNotification(eventId: ID!, notificationId: ID!): ID
}
