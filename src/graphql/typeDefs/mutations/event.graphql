type EventMutation {
  update(event: EventUpdateInput!): Event! @auth(requires: "events")
  notifications: NotificationsMutation
    @auth(requires: "events:event:notifications")
  milestones: MilestonesMutation @auth(requires: "events:event:milestones")
}
